name: Build KDE Connect (Nightly)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    container:
      image: ghcr.io/justsaft/vanilla-ci-building:main

    env:
      DEBIAN_FRONTEND: noninteractive
      KDE_SRC: /opt/kde/src
      KDE_BUILD: /opt/kde/build
      KDE_INSTALL: /opt/kde/usr

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      # -------------------------
      # Cache KDE sources + build
      - name: Restore KDE cache
        uses: actions/cache@v4
        with:
          path: |
            /opt/kde/src
            /opt/kde/build
          key: kde-${{ runner.os }}-${{ hashFiles('kdesrc-buildrc') }}
          restore-keys: |
            kde-${{ runner.os }}-

      # -------------------------
      # Setup kdesrc-build
      - name: Setup kdesrc-build
        run: |
          echo "/usr/kdesrc-build" >> $GITHUB_PATH

      # -------------------------
      # Write kdesrc-build config
      - name: Create config
        if: false
        run: |
          cat << 'EOF' > kdesrc-buildrc
          global
              set-env MAKEFLAGS -j${num-cores}
              source-dir /opt/kde/src
              build-dir /opt/kde/build
              install-dir /opt/kde/usr
              cmake-generator Ninja
              cmake-options -DCMAKE_BUILD_TYPE=Release -DBUILD_PYTHON_BINDINGS=OFF
              branch-group kf6-qt6
              do-not-compile python
          end global

          module kdeconnect-kde
              repository kdeconnect-kde
              include-dependencies true
          end module
          EOF

      # -------------------------
      # Build KDE Connect
      - name: Build KDE Connect (nightly)
        run: |
          kdesrc-build --generate-config
          kdesrc-build --include-dependencies --reconfigure --async kdeconnect
          ldd /opt/kde/usr/kdeconnect-kde > /opt/kdeconnect.txt

      # -------------------------
      # Package artifacts
      - name: Create archives
        run: |
          tar -czf kdeconnect-nightly.tar.gz -C /opt/kde/usr . || true
          tar -czf logs.tar.gz -C /github/home/kde/src/log/latest/ .

      # -------------------------
      # Upload log archive
      - name: Upload log archive artifact
        uses: actions/upload-artifact@v4
        with:
          name: kdeconnect-nightly-build-logs
          path: logs.tar.gz

      # -------------------------
      # Upload artifact
      - name: Upload artifact
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: kdeconnect-nightly
          path: |
            kdeconnect-nightly.tar.gz
            /opt/kdeconnect.txt

      # -------------------------
      # Save updated cache
      - name: Save KDE cache
        uses: actions/cache@v4
        with:
          path: |
            /opt/kde/src
            /opt/kde/build
          key: kde-${{ runner.os }}-${{ github.run_id }}